// Service Worker - placeholder
// Este arquivo existe apenas para evitar erros 404 no console
self.addEventListener('install', () => self.skipWaiting());
self.addEventListener('activate', () => {});
